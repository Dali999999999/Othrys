/// Standard modal dialog widths and heights for Othrys.
class AppDialogSize {
  AppDialogSize._();

  /// Compact modals: Port forwarding, quick confirmations (460.0px).
  static const double compactWidth = 460.0;

  /// Standard forms: Add/Edit Server, Command Palette (520.0px).
  static const double standardWidth = 520.0;

  /// Wide modals: SFTP File Editor, Logs Viewer (740.0px).
  static const double wideWidth = 740.0;

  /// Standard height for wide content dialogs (480.0px).
  static const double wideHeight = 480.0;
}
